const upperCaseLetters = (str) => {
    return str.toUpperCase(); // Converting the string to uppercase.
};

export default upperCaseLetters; //Exporting the function upperCaseLetters to be used in other files.